apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: product-service
  namespace: ecommerce-dev
spec:
  selector:
    matchLabels:
      app: product-service

  namespaceSelector:
    matchNames:
      - ecommerce-dev

  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s