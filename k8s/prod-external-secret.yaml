apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: app-secret
  namespace: ecommerce-prod
spec:
  refreshInterval: 1h

  secretStoreRef:
    name: aws-secret-store
    kind: ClusterSecretStore

  target:
    name: app-secret

  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: prod/ecommerce/secret
        property: DATABASE_URL